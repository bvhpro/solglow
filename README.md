# solglow
SolGlow is an experimental React Native app (Android-first) that turns NFTs in a user's Solana wallet into dynamic wallpapers. It demonstrates core flows: wallet connect, fetch NFTs, select wallpaper, and an Android native module to set wallpaper.
